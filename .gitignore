node_modules
dist
bin/
coverage/
.turbo
.DS_Store
.idea
.env
.env.*
!.env.example
.env.local
.env.*.local

# Local session scratch — answers/reports, organized as answers/NN-session-name/.
# Never committed. Not auto-cleaned; removed only on explicit request.
answers/
