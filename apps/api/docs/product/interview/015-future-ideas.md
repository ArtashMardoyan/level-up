# 015 - Future Ideas

Beyond the roadmap in `014` — not committed, just parked:

- **English coaching** — grammar/vocabulary rubric axes + feedback, the Learning Profile (`007`) and
  personalized Dictionary (`008`). Deferred out of the MVP; the anchor of `014` v2.
- **Voice / audio interviews** — answer and hear questions by voice (speech-to-text + TTS). The
  `question_translations.audio` MP3-per-language keys already exist for this. (User-requested "full
  support".)
- **AI strong/weak analysis & question targeting** — mine the per-question scores captured from the
  MVP (`010`) so the AI learns each user's strong/weak topics and recommends which questions to ask
  next. This is the payoff of storing per-question results from day one; formalized by the Learning
  Profile (`007`). (User-requested.)
- **Full multilingual support** — beyond RU/EN (the MVP is already bilingual), add more languages
  via new `question_translations` rows.
- AI avatar interviewer
- Live coding interviews (in-browser editor)
- System Design interviews
- Company-specific interview tracks (e.g. FAANG-style)
- Weekly AI coaching reports (tie into the notifications feed)
- Deeper gamification on top of the existing streak (badges, leagues)
- Team / pair practice mode
- AI career roadmap
- Adaptive difficulty that reacts to the Learning Profile mid-interview
